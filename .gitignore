# dot files
.DS_Store

# Node.js
node_modules
pnpm-lock.yaml
yarn.lock

# TSC
tsconfig.tsbuildinfo

# Scaffold
.env
.scaffold
build
logs
# local session archive (存档) — never publish
session-notes.md
# local research corpus (phase-A reports) — never publish
.research
