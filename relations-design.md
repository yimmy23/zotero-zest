# Zest / Refs：可解释的论文与标注关系

2026-09-08。本文是联合重构设计，不代表语义检索、向量模型或跨插件接口已经实现。第一阶段只修复已有关系的正确性；保留原视图，不发布、不接入新的外部文本处理服务。

## 1. 先区分关系含义

| 关系     | 依据                              | 适合的入口               | 不能推出什么                                           |
| -------- | --------------------------------- | ------------------------ | ------------------------------------------------------ |
| 人工关联 | 用户已保存的 Zotero Related       | Zest 文库关系图          | 不代表引用或主题相同                                   |
| 组织关系 | 共享作者、标签、分类              | Zest 文库关系图          | 同一文件夹不代表内容相关；姓名兼容不等于身份确认       |
| 文本相似 | 标题 / 摘要或具体标注的向量、词项 | Zest 相似文献 / 相似标注 | 不代表观点一致、因果联系或证据等级                     |
| 直接引用 | 明确的 citing → cited 标识及来源  | Refs 引文图谱            | 引用不等于支持其结论                                   |
| 文献耦合 | 两篇论文共享参考文献              | Refs 引文图谱            | 不是共被引；也不是直接引用                             |
| 共被引   | 第三方论文同时引用这两篇          | Refs 后续可选分析        | 新论文共被引少不代表不相关                             |
| 来源推荐 | OpenAlex 等返回的 relatedWorks    | Refs 相关研究            | 来源未公开的精确打分不能冒称我们已验证的语义或引用证据 |

Zest 负责“我已收藏、阅读和标注的内容如何联系”，Refs 负责“这篇论文的引文脉络与库外发现”。二者共用关系术语和身份规则，不共用 UI 状态，也不强制互相安装。

## 2. Smart Connections 借鉴的是什么

核对了官方当前文档、主仓库 `package.json`（4.7.2）、`src/items/connections_list.js`，以及其声明的 jsbrains 固定版本中的 Markdown block 输入构造。没有把过时设置页当作当前功能清单。

基本流程是：文本 → embedding 模型 → 向量 → 相似候选 → 过滤 / 排序 → 用户审阅。其基础打分为余弦相似度：

`cos(a,b) = dot(a,b) / (norm(a) * norm(b))`

向量来自模型学到的文本表示。计算比较的是表示空间中的方向；经过单位长度归一化后，余弦就等于点积。它能在模型能力范围内找出用词不同但含义接近的内容，不需要两段文字有显式链接。不同语言的向量是否可比取决于模型是否支持跨语言对齐，不能由“都是向量”推断。

余弦原始理论范围为 −1 到 1；实际分布取决于模型和文本。空向量、零范数、非有限值、不同模型 / 版本 / 维数都必须拒绝比较。分数不是相关概率，0.8 不能读成“80% 相同”或医学结论一致。

官方代码还提供候选 top-k、过滤后不足时扩大召回、可选二次排序、置顶和隐藏等机制；当前结果代码存在小分值整组缩放。因此界面上的数字未必就是可跨查询比较的原始余弦。我们保留原始分数和算法版本，不照搬显示分数处理。

值得采用的设计：

- **双粒度**：整篇论文检索与片段检索分开。Smart Connections 区分 Sources / Blocks；其 Markdown block 输入包含 breadcrumbs 与块正文。
- **候选先于可视化**：侧栏以可读的相似结果列表为主，小图辅助定位，不先铺全库的线。
- **人工确认才固化**：相似连线是建议，不自动写入 Zotero Related、笔记或标签。
- **可暂停、可检查覆盖率**：索引缺失与“没有相似结果”分别呈现；隐藏侧栏不持续计算。

不直接 fork 或复制当前实现：主仓库当前许可证包含竞争产品限制，不应未经许可兼容性评估移植到公开 AGPL 插件。此处只采用通用算法思想并独立实现；不是对许可适用性的法律结论。

来源：[当前文档](https://smartconnections.app/docs/connections/)、[结果计算代码](https://github.com/brianpetro/obsidian-smart-connections/blob/main/src/items/connections_list.js)、[块输入构造](https://github.com/brianpetro/jsbrains/blob/ba80525b083cf515555cedbdc6294a2d0aa5da68/smart-blocks/actions/get_embed_input/markdown.js)、[许可证](https://github.com/brianpetro/obsidian-smart-connections/blob/main/LICENSE)。

## 3. 面向论文的算法组合

### 3.1 语义通道：先检索，后验证

论文表示使用原始标题和摘要，保留语言与缺失标记；不将作者列表、期刊名、参考文献表、阅读状态一起塞入正文向量。超长摘要按结构分段，保留分段依据；不静默截断后还显示“已完整索引”。

标注表示使用原文摘录，评论作为独立字段，并可加入明确分隔的论文标题 / 章节上下文。每个结果保留 libraryID、itemKey、attachmentKey、annotationKey、页码和跳转链接。不得将评论当成论文原话，也不将多个片段的高分自动解释为整篇论文相似。

推荐先建立一个不依赖模型的词项基线，再比较小型多语言 embedding 与科学文献 embedding。SPECTER2 可作为英文科学文献候选，但其论文不能证明它在本用户的中文医学标注上最好。中英文跨语种与领域缩写要单独评测。模型选择在实测后确定，不能仅按下载热度或模型体积决定。

针对“淋巴结评估”，检索可以尝试召回关于淋巴结取样、清扫、分站和病理评估的片段；这是预期用途，不是已验证的召回结果。临床试验名称、基因符号、站号等精确词项另设词法通道，以减少向量模型将关键差异模糊化的问题。否定句、相反结果、N1/N2 等差异必须进入对抗验收集。

### 3.2 引文通道：保留共享数，另算归一化强度

文献耦合可用二值参考文献向量的余弦作为透明基线：

`coupling(A,B) = |references(A) ∩ references(B)| / sqrt(|references(A)| * |references(B)|)`

共享数与归一化分数都保留。这样能减轻长参考文献表天然占优的影响，但不保证消除领域、年代或数据库覆盖偏差。参考文献未知与确认空列表不同；只有已观察列表的比较才可计算，部分覆盖要注明。先去重合法标识，再计算分子 / 分母。

VOSviewer 的 association-strength 归一化值得作为对照，不能把任意“共同 3 篇”阈值当成学术相关性的公认界线。共被引需要额外的入引数据，获取成本与新文献的时间滞后更大，列为后续增强，不作为第一版必需项。

Connected Papers 官方说明其关系度量借鉴共被引和文献耦合，并明确图不是引文树；这支持关系类型分层设计，而不是复制其未公开的完整参数。

来源：[Connected Papers](https://www.connectedpapers.com/about)、[归一化研究，DOI 10.1002/asi.21075](https://onlinelibrary.wiley.com/doi/10.1002/asi.21075)、[VOSviewer 手册](https://www.vosviewer.com/documentation/manual_vosviewer_1.6.20.pdf)、[SPECTER2，DOI 10.18653/v1/2023.emnlp-main.338](https://aclanthology.org/2023.emnlp-main.338/)。

### 3.3 暂不设置一个任意加权总分

第一版分别展示“文本相近”“共享参考文献”“直接引用”和“人工关联”。同一对论文可同时有多种关系。不要直接规定“语义 50%＋引用 30%＋作者 20%”，因为这些量纲、数据覆盖和用途不同，权重尚无本地评测依据。

后续若混合排序，比较按名次融合等方法，并保留各通道原始证据、缺失字段和检索范围。引用数、期刊分区、阅读时间不作为相似度乘数。人工隐藏或置顶仅改变用户自己的显示偏好，不静默训练成全局“学术相关性”。

## 4. 最小代码边界

沿用现有 builder / view 分离结构，不引入新框架或常驻服务。

1. **身份与输入**：保留 Zest 的同库 itemKey 身份与 Refs 的严格标识匹配；相似标题不自动合并文献。作者匹配要求姓名兼容且双向唯一，无按人数 / 位次强配。
2. **关系构建**：每条关系保存 type、source、target、方向、来源和证据字段；手动链接与算法候选分离。数据对象不能被布局坐标写回缓存。
3. **选图**：候选预算与画面预算分离。当前论文视图先保留中心的一阶邻居；概览保留多个分组覆盖。每节点 top-k 及总预算限制拥挤，k 由实测决定；不为了连通而添加不存在的边。
4. **渲染**：直接引用显示箭头，来源推荐和相似关系使用不同线型；点击关系可看依据。聚类颜色只表示当前算法分组，不代表研究共识或医学分类。

Leiden 可用于后续主题聚类；它解决的是已构建网络的社区划分，不负责判断两篇论文是否相关。先保证边可信，再评估聚类，避免用更漂亮的图掩盖错误连线。[原始论文，DOI 10.1038/s41598-019-41695-z](https://www.nature.com/articles/s41598-019-41695-z)

### 跨插件接口（设计，尚未实现）

Refs 暴露带版本号的只读关系快照：请求包含 libraryID、已确认论文标识和 `allowNetwork: false`；响应包含来源、获取时间、覆盖状态及关系。Zest 不读取 Refs 私有缓存文件，不代替 Refs 自动补抓。对方未安装或接口不兼容时，仅隐藏对应增强入口，原功能仍可用。

可增加明确的“在 Refs 探索引文”和“在 Zest 查看本地标注”交接动作；传递文献身份，不共享选中状态、DOM 或实例生命周期。接入协议需在两端通过缺插件 / 升级 / 多窗口测试后才启用。

## 5. 性能与数据边界

- 内容哈希、预处理版本、模型标识 / 版本、维数共同决定向量缓存身份；模型切换不混用旧向量。
- 增量处理变更条目，删除 / 合并 / 移库清理失效索引；错误任务可重试但不清除服务端退避。
- 单中心查询先用矩阵点积 / 有界 top-k 实测，不创建全库 N² 的永久边表；规模确有需要时再引入近似近邻索引，并评估召回损失。
- 切条目、切库、关闭或隐藏时取消过期任务；不仅丢弃最终结果，还要停止未发出的批次。同步计算应分块让出主线程。
- 用户明确启用后才准备模型和索引；本地优先。云端处理另行确认具体模型、接收方、字段和费用，标注 / 评论默认不外发。此次没有下载或运行任何 embedding 模型。
- 覆盖率、输入数量、耗时、取消次数可观测，但日志不记录私有原文或凭证。数据缺失、模型不可用与“没有达到阈值的结果”分别处理。

## 6. 分期与验收

### 当前第一阶段：修正确实存在的错误

- Zest：拒绝同姓不同名及作者位次强配，共用严格作者匹配规则。
- Refs：区分引用 / 推荐 / 文献耦合；修正引证方向，保留多角色和共享参考文献数。

用户随后授权改进 Refs 相关文献算法和图标，增加一项有界实现：同时获取已有 Semantic Scholar / OpenAlex 推荐接口，每源最多 40 个候选；先收到的来源先显示，最终按固定来源顺序重新融合。每个来源的同一论文只贡献一次，保留原始列表位次。采用 `sum(1 / (60 + rank))` 的倒数排名融合（RRF）作为透明基线，不按被引数加权；`60` 来自原论文的基线参数，不代表已在本地医学任务中调优。OpenAlex 列表顺序未被此处验证为严格相关性排名，因此界面只称“来源列表位次”，不冒称原始模型相似度。人工关联仍独立优先展示；排除本文及人工关联后最多显示 20 条推荐。

身份合并只采用标准化且兼容的共享标识，任一共享命名空间显式冲突即否决；不启用标题身份兜底。无标识的同名记录可能保留重复，这是当前保守边界。每条推荐保留来源 / 位次证据，排名不写文库。有效空列表与服务不可用分开，切换条目 / 折叠后不接收旧结果，OpenAlex 下一阶段请求可跳过；不强行取消其他窗口共享的 HTTP 请求。完整结果采用最多 40 个条目、固定 30 分钟的内存缓存，部分失败不缓存为完整结果。更新仍可能复用 HTTP 层缓存，不宣称每次强制重新联网。

来源：[RRF 原论文，DOI 10.1145/1571941.1572114](https://cormack.uwaterloo.ca/cormacksigir09-rrf.pdf)、[Semantic Scholar 推荐接口](https://api.semanticscholar.org/api-docs/recommendations)、[OpenAlex 数据关系](https://help.openalex.org/how-to/api-recipes/)。该实现不是 Smart Connections 风格的本地文本向量索引，尚不能宣称真实推荐准确率提高。

- 纯内存回归覆盖冲突姓名、模糊首字母、反向引用、推荐不冒充引用、多角色与去重；构建、原生侧栏、两主题和卸载不退化。

### 第二阶段：本地相似文献 / 标注原型

对词项基线、embedding、混合召回做同一范围下的盲审比较。固定一批代表性中英文查询与文献 / 标注，分别记录前 10 项的人工相关性、遗漏、重复、来源跳转正确性；评价标准由实际研究任务决定。未经人工判定的数据不能宣称准确率提高。

性能测试建议覆盖 1,000 / 10,000 篇元数据和 10,000 / 100,000 条标注，报告本机冷启动、热查询的 p50 / p95、内存峰值和主线程长任务，不将目标写成已达到。使用合成数据或获准本地样本，不读取 / 外发日常文库作为隐含授权。

### 第三阶段：联合证据与图谱增强

实现只读跨插件交接、归一化耦合、分阶段网络预算和取消，再按实际价值评估共被引、重排及主题聚类。所有阶段保留旧模式和可回退路径；检索建议始终需要用户审阅，不自动写库。
